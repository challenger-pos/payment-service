spring:
  application:
    name: billing-service

  cloud:
    aws:
      region:
        static: us-east-2
      credentials:
        access-key: test-key
        secret-key: test-secret
      stack:
        auto: false
      sqs:
        enabled: false

# Test configurations
aws:
  region: us-east-2
  dynamodb:
    endpoint: http://localhost:8000
  credentials:
    access-key: test-key
    secret-key: test-secret
  sqs:
    payment-request-queue: test-payment-request-queue
    payment-request-dlq: test-payment-request-dlq
    payment-response-success-queue: test-payment-response-success-queue
    payment-response-failure-queue: test-payment-response-failure-queue
    auto-create-queue: false
    max-receive-count: 3

# DynamoDB Configuration for Tests
dynamodb:
  table-name: payments-test
